import { FiltrousuariosPipe } from './filtrousuarios.pipe';

describe('FiltrousuariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrousuariosPipe();
    expect(pipe).toBeTruthy();
  });
});
